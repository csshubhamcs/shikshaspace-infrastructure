apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactive-microservice.fullname" . }}
  labels:
    {{- include "reactive-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactive-microservice.selectorLabels" . | nindent 4 }}